// Runs on frontend only (enqueued via "viewScript" in block.json).
document.addEventListener('DOMContentLoaded', () => {

    console.log('FAQ Section');
    // document.querySelectorAll('.acme-section-wrapper').forEach((el) => {
    //   // do something progressive-enhancementy here
    // });
});
  